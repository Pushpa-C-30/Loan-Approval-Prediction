# LoanLens project instructions

- Frontend: React with Vite in `src/`.
- Backend: Express API in `server/index.js`.
- Keep loan approval rules isolated in the backend `analyzeLoan` function.
- Use `npm run dev` to run the frontend and backend together.
- Keep the approval result transparent by returning checkpoint details from `/api/analyze`.
